'use strict'

const ENUM_VALIDATION = {
    IS_LENGTH: 'is_length',
    IS_NUMERIC: 'is_numeric',
    IS_DATE: 'is_date',
    IS_REGEX: 'is_regex',
    IS_EMAIL: 'is_email',
};

module.exports = ENUM_VALIDATION;

